# Environment variables for notes_backend

# If provided, the app will use PostgreSQL via Npgsql; otherwise it falls back to SQLite (notes.db)
# Example for container named 'notes_database':
# NOTES_DB_CONNECTION=Host=notes_database;Port=5432;Database=notes;Username=postgres;Password=postgres;SSL Mode=Disable;Timeout=15;Command Timeout=30
NOTES_DB_CONNECTION=
